If the plugin still doesn't work:

Did you unblock the dll? That could prevent it from being loaded. Right click the dll -> Properties.

Windows likes to block files that can contain executable code.  So mainly .exe and .dll files.